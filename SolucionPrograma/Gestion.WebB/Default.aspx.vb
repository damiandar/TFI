
Partial Class _Default
    Inherits System.Web.UI.Page

    Protected Sub Page_Load(sender As Object, e As System.EventArgs) Handles Me.Load
        'Dim lista As New List(Of Integer)
        'For i = 0 To 50
        '    lista.Add(i)
        'Next
        'If lista.Count > 15 Then
        '    Dim elementosaborrar As Integer = lista.Count - 15
        '    lista.RemoveRange(15, lista.Count - 15)
        'End If
        'Response.Write(lista.Count.ToString() & "<br/>")
        'For Each num As Integer In lista
        '    Response.Write(num.ToString() & "<br/>")
        'Next
    End Sub

End Class
